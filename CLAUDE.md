# nicookie

Fork of `@steipete/sweet-cookie` (Steipete, MIT). Node >=22 or Bun; pnpm
workspace. The CLI entrypoint builds to `packages/core/dist/cli.js`.

After changes run the narrow checks: `pnpm check`, `pnpm test`, `pnpm test:bun`,
`pnpm lint`, `pnpm build`.
